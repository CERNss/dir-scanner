# --- Python 基础 ---
# 字节码编译文件（不需要版本控制）
__pycache__/
*.pyc

# --- 虚拟环境 ---
# 避免把庞大的库文件上传
venv/
env/
.env

# --- PyInstaller 打包产物 (非常重要) ---
# 打包过程中生成的临时文件夹
build/
# 最终生成的 exe 所在的文件夹
dist/
# 打包配置文件 (如果你不想保留打包配置，可以忽略)
*.spec

# --- 运行时生成的数据 ---
# 脚本跑出来的 Excel 结果
文件统计清单.xlsx
# Excel 打开时产生的临时锁文件 (以 ~$ 开头)
~$*.xlsx

# --- IDE 配置 (可选) ---
# VS Code / PyCharm 的本地配置
.vscode/
.idea/